import React from 'react'
import logo from '../assets/images/logo.png'

const Header = () => {
  return (
    <header>
      <nav>
        <img src={logo} alt="" />
      </nav>
    </header>
  )
}

export default Header